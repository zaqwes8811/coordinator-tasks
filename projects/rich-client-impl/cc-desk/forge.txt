Idea:
  сделать как у Спольски и у меня в Гуглотаблицах
  сделать как на гитхабе, слева возможные теги, и как у спольски слева

Wariants:
- проблеме в том как содержать быть с синхронизацией
  позволять ли быть приложению в рассогласованном состоянии
  относительно долго. В редактором можно же так работать.
  на делать же запросы в базу на каждый чих.

Entities:
- записи - и тут вопросы сразу - если использовать shared_ptrs то как сделать чтобы они не утекли. контейнер должен владеть и синхронизировать с базой данных. по идее контейнер лучши инкапсулировать. суть в том что если 
указатель шарит, то не так то просто удалить запись.
- теги тоже - их в таблицу все равно не запихать
- проблемы с константностью и умными указателями.

Values:
- отображения

Aggregates:
- запись с тегами - сделать генератор

Queries:
- запрос по задачам - с тегами
- выборка по тегам
- нужно еще удалять тег как тип. т.е. удалять все ссылки

Arch:
- пока один запрос - получить все записи, пока без тега

View:
- слева фильтры, права сетка
- при нажатии на ячейку можно открывать другую панель - типа для нее только, а
  так работать с фильтрами
- столбцы пока не перемещаемые - позиции заданы жестко снизу

Async and parallel:
- пока не ясно зачем. реализация логики прямая, параллельно много делать пока не придется
- с другой стороны сохр. в базе может быть асинхронной операцией

Hotkeys:
- Ctrl+S

Error handling:
- базовый assert плох. может упасть и попортить базу данных
  хотя возможно исключение не решение, хотя
- все действия в работе через слоты, там и буду ловить исключения

Impl stranges:
- segfault in boost::shared_ptr несколько раз в атомарных операциях, потом пропало - может остатки кланга

Cases:
- добавить запись - лучше сделать все как в гуглотаблицах - просто добавлять строки

MVC
- http://javascript.ru/optimize/antimvc
- будет только Model/View http://qt-project.org/doc/qt-4.8/model-view-programming.html
  в qt уже заложено то что нужно, в принципе все что нужно для общения с базой данных похоже тоже есть
  Но скребет то, что привязка к Qt. Не всегда хотелось бы.
